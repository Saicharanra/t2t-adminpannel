export const siteConfig = {
  name: "T2T Admin",
  description: "Trash2Treasure Admin Panel — Internal operations platform",
  url: process.env.NEXT_PUBLIC_APP_URL || "http://localhost:3000",
  version: "1.0.0",
} as const;
